odoo.define('tobako_pos_keymap.PaymentButton', function (require) {
    'use strict'

    const ActionpadWidget = require('point_of_sale.ActionpadWidget')

    ActionpadWidget.template = 'PaymentButton'
})